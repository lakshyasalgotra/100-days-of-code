//Q32: Write a program to check if a number is a palindrome.

/*
Sample Test Cases:
Input 1:
121
Output 1:
Palindrome

Input 2:
123
Output 2:
Not palindrome

*/

#include <stdio.h>

int main() {
    int n, original, reverse = 0, remainder;
    scanf("%d", &n);

    original = n;  // Store original number to compare later

    while (n != 0) {
        remainder = n % 10;               // Get last digit
        reverse = reverse * 10 + remainder; // Append digit to reverse
        n = n / 10;                      // Remove last digit
    }

    if (reverse == original)
        printf("Palindrome\n");
    else
        printf("Not palindrome\n");

    return 0;
}

Q29-100 Days Of CodeQ29-100 Days Of CodeQ29-100 Days Of Code
